{
  "name": "preactCalculator",
  "version": "1.0.0",
  "description": "Example calculator built with Preact",
  "main": "index.js",
  "scripts": {
    "test": "mocha && eslint src",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "keywords": [],
  "author": "Michael Desantis",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "chai": "^4.1.1",
    "eslint": "^4.5.0",
    "eslint-config-synacor": "^1.1.0",
    "if-env": "^1.0.0",
    "preact-cli": "^1.4.1"
  },
  "dependencies": {
    "preact": "^8.2.4",
    "preact-compat": "^3.17.0",
    "preact-router": "^2.5.7"
  }
}
